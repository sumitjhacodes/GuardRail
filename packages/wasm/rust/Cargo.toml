[package]
name = "guardrail_wasm"
version = "0.3.0"
edition = "2021"
description = "Optional Rust WASM accelerator for Guardrail (requires wasm-pack)"
license = "MIT"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2"

[profile.release]
opt-level = "s"
lto = true
